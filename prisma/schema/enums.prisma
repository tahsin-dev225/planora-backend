enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

enum UserStatus {
  ACTIVE
  INACTIVE
  BANNED
}

enum EventType {
  PUBLIC
  PRIVATE
}

enum ParticipantStatus {
  PENDING
  NEED_PAYMENT
  APPROVED
  REJECTED
}

enum PaymentStatus {
  PENDING
  SUCCESS
  FAILED
}
